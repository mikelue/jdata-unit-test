%TAG !jdut! tag:jdut.mikelue.guru:1.0/
%TAG !dbtype! tag:jdut.mikelue.guru:jdbcType:1.8/
%TAG !sql! tag:jdut.mikelue.guru:sql:1.0/

---

- !sql!table ex_artist : {
    config: {
        build_operation: "INSERT", # Build data
        clean_operation: "NONE" # Do nothing(the tested SQL is to remove built data)
    },

    data: [
        { "at_id": 38971, "at_name": "Blue Mountain" }
    ]
}
